import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  idInput: {
    padding: 12,
    borderWidth: 1,
    borderColor: '#efefef',
    borderRadius: 40,
    marginBottom: 8,
  },
  pwInput: {
    padding: 12,
    borderWidth: 1,
    borderColor: '#efefef',
    borderRadius: 40,
  },
});
