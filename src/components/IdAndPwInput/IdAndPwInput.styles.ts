import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  idInput: {
    height: 52,
    borderWidth: 1,
    paddingHorizontal: 16,
    borderColor: '#efefef',
    borderRadius: 40,
    marginBottom: 8,
  },
  pwInput: {
    height: 52,
    paddingHorizontal: 16,
    borderWidth: 1,
    borderColor: '#efefef',
    borderRadius: 40,
  },
});
